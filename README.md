# This is a simple Python program that generates the name of your band.
# During execution, the compiler (or rather, the program itself) will ask you 
# a series of questions. Based on your answers, a unique band name will be created.
